# Secrets — never commit real credentials
.env

# Local vector store data (rebuildable by re-running /ingest)
chroma_data/

# PRIVATE — analyzed companies, their report data, outreach, rendered pages.
# These contain third-party company analysis + founder contacts. NEVER commit.
reports/
outreach.xlsx
web/dist/
evals/vision_out/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
.vscode/
# Office temp/lock files (e.g. ~$outreach.xlsx while open)
~$*
# code-review-graph tool artifacts — local dev aid, never commit
.code-review-graph/
.claude/
.mcp.json
CLAUDE.md

# netlify deploy cache
.netlify/
